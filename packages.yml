---
- name: Install packages playbook
  hosts: workstation
  become: true
  gather_facts: true
  
  tasks:
    - name: Install packages task
      ansible.builtin.apt:
        name:
          - ansible
          - htop
          - python3
          - python3-psutil
          - python3-venv
          - rsync
        state: present

