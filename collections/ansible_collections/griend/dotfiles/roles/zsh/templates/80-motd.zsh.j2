# clear
echo "Distro: {{ ansible_distribution }} {{ ansible_distribution_version }} ({{ ansible_distribution_release }})"
echo "Host:   {{ ansible_fqdn }} / {{ ansible_default_ipv4.address }}"

if [ -x /usr/games/fortune ] ; then
    echo ""
    /usr/games/fortune
    echo ""
fi

echo ""

apt list --upgradable
