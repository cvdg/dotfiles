---
- name: Install local
  hosts: localhost
  gather_facts: true

  roles:
    - role: common
      tags: common

    - role: git
      tags: git

    - role: bin
      tags: bin

    - role: zsh
      tags: zsh
